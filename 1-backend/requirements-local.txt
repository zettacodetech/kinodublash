# ===== OG'IR LOKAL AI MODELLARI (ixtiyoriy, GPU tavsiya etiladi) =====
# O'rnatish:  pip install -r requirements-local.txt
# DIQQAT: bular ~10-20GB joy va ko'p RAM/VRAM talab qiladi.

# --- STT (alternativ) ---
openai-whisper==20240930

# --- Embeddings (bge, nomic, MiniLM, mxbai) ---
sentence-transformers==3.3.1

# --- Rasm generatsiya (SDXL, FLUX, PixArt, turbo) ---
diffusers==0.32.1
transformers==4.47.1
accelerate==1.2.1
torch==2.5.1

# --- Vision (BLIP, PaliGemma) ---
pillow==11.0.0

# --- Qo'shimcha TTS ---
coqui-tts==0.25.1         # XTTS v2
# bark — GitHub'dan:  pip install git+https://github.com/suno-ai/bark.git
scipy==1.14.1             # bark audio yozish uchun
chattts==0.2.1
torchaudio==2.5.1
# melotts — GitHub'dan:  pip install git+https://github.com/myshell-ai/MeloTTS.git
