# ===== ASOSIY (har doim kerak) =====
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0
pydantic==2.10.4
pydantic-settings==2.7.1
python-dotenv==1.0.1
httpx==0.28.1              # Ollama va umumiy HTTP

# ===== DEFAULT PROVAYDERLAR (yengil) =====
edge-tts>=7.2.8           # TTS: bepul, native o'zbek ovozlari (403 fix uchun >=7.2.8)
faster-whisper==1.1.0     # STT: lokal, tez

# ===== VIDEO MANBALARI VA TAHLIL =====
# yt-dlp NIGHTLY o'rnatiladi (Dockerfile'da) — YouTube tez-tez o'zgaradi,
# stable versiya "No video formats found" berishi mumkin. numpy — ovoz jinsi tahlili.
numpy>=1.26,<2.2

# ===== BULUT PROVAYDERLAR (ixtiyoriy) =====
groq==0.13.1              # STT/tarjima: Groq
openai==1.59.6            # TTS: OpenAI

# Ollama (llama3.2/qwen2.5/gemma2/mistral, coder, llava) — pip EMAS:
#   https://ollama.com dan o'rnatiladi, keyin `ollama pull <model>`.

# ===== OG'IR LOKAL MODELLAR uchun: requirements-local.txt ni o'rnating =====
